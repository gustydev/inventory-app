extends layout 

block content 
    h2= title 
    a(href='/inventory/category/create' style='font-weight: bold') Create category
    if categories.length 
      ul
        for category in categories 
          li
            a(href=`./category/${category._id}`)= category.name
    else 
      p No categories found
    